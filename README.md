# UnrealSharpGAS
Work in progress `GameplayAbilitySystem` bindings for C# using UnrealSharp.

Documentation will come soon.

# Setup
Clone to `ProjectRoot/Plugins` and compile the project's C++ solution.
